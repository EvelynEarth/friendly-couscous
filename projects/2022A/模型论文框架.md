# 2022A 模型论文框架

> 当前文件仅保存项目 2022A 的有效模型语义与阶段状态；历史由 Git 保存。具体正式数值以后只以用户本地 full_fidelity 运行并通过质量门的工作簿为准。

- 项目：`2022A`
- 竞赛与题号：`2022 年高教社杯全国大学生数学建模竞赛 A 题——波浪能最大输出功率设计`
- 框架版本：`v0.8-project-memory`
- 框架模式：`compact`
- 当前阶段：`model_design`
- 当前状态：`current`
- 当前求解边界：`只锁定 Q1；Q2-Q4 暂不随本次批准自动锁定`

## 当前有效口径

### 题目对象与核心关系

- 研究对象：浮子—振子—PTO 波浪能装置。
- Q1 只考虑浮子垂荡与振子沿竖直中轴的往复运动。
- 波浪激励、附加质量、兴波阻尼采用附件 3 在 `ω=1.4005 s^-1` 对应参数；装置质量、尺寸、海水密度、重力加速度和弹簧刚度采用附件 4。
- 坐标均以静水静平衡位置为零点，向上为正；因此重力、静浮力与弹簧静预压的常量项在增量方程中相消。
- `preprocessing_decision = not_needed`：附件为结构化参数表和结果模板，只做字段、单位、缺失/非有限值、模板表头等非破坏性检查，不进行插值、滤波、平滑、标准化或异常删除。
- 核心答案属于高精度数值项：结果工作簿与论文关键数值至少保留 6 位小数。

### 各问依赖关系

| 小问 | 当前目标 | 当前状态 | 本次是否锁定 |
|---|---|---|---|
| Q1 | 给定两类直线阻尼，求前 40 个波浪周期内垂荡位移和速度时程 | designed | 是 |
| Q2 | 在垂荡模型上优化阻尼参数以最大化平均输出功率 | pending | 否 |
| Q3 | 建立垂荡+纵摇耦合模型并求时程 | pending | 否 |
| Q4 | 优化直线与旋转阻尼 | pending | 否 |

### Q1：浮子与振子的垂荡运动

- 主/次题型：`simulation / physical_mechanism`
- 当前状态：`designed`
- Problem Contract：`frozen`
- 语义闭环：`passed`
- 公式推理链：`passed`
- 复杂度复审：`passed`
- Model Challenge：`passed`
- Human Model Approval：`pending`
- semantic revision：`1`
- semantic hash：`3c949a24b44d6d9ee12abe4e763e47942406456c523faf295263f06fac297192`
- semantic change categories：`initial_design`
- 核心模型收束：`required`
- 算法流程呈现：`not_needed`

#### 当前模型口径

**Problem Contract**

| 项目 | 当前冻结口径 | 来源/推导依据 |
|---|---|---|
| 研究对象 | 浮子与振子构成的两自由度垂荡系统 | A题.pdf 问题1 |
| 输入 | 附件3 Q1 行、附件4、两类阻尼规则 | A题.pdf + 附件3.xlsx + 附件4.xlsx |
| 输出 | `result1-1.xlsx`、`result1-2.xlsx` 所要求的时间、浮子位移/速度、振子位移/速度；论文给出 10/20/40/60/100 s 数值 | A题.pdf 问题1 |
| 时间域 | `T=2π/ω`，前 `40T`；输出网格 `0,0.2,...,floor(40T/0.2)*0.2` | 题面“前40个波浪周期内时间间隔为0.2 s” |
| 初值 | `z_f(0)=z_o(0)=ż_f(0)=ż_o(0)=0` | 题面“初始时刻浮子和振子平衡于静水中” |
| 不允许的简化 | 不删除附加质量、兴波阻尼、静水恢复力或 PTO 相互作用；不把幂律阻尼系数误写为幂律阻尼力 | 题面物理定义与语义闭环 |

**变量、参数与模型**

- `z_f(t)`：浮子相对静平衡位置的垂荡位移，m。
- `z_o(t)`：振子相对静平衡位置的垂荡位移，m。
- `v_r = ż_f-ż_o`：浮子相对振子的速度。
- 浮子静水恢复刚度：`K_h = ρ g πR^2`。

常量阻尼与非线性阻尼共用动力学骨架：

```text
(m_f+m_a) z̈_f = f cos(ωt) - b ż_f - K_h z_f - k(z_f-z_o) - F_d
m_o z̈_o       = k(z_f-z_o) + F_d
```

其中：

```text
Case 1: F_d = c v_r,                  c = 10000 N·s/m
Case 2: c(v_r)=a|v_r|^n,
        F_d = a|v_r|^n v_r,           a = 10000, n = 0.5
```

这里采用 `F_d=a|v_r|^n v_r`，因为题面说的是“阻尼系数”与相对速度绝对值的幂成正比，而阻尼力仍等于阻尼系数乘相对速度。

### Formula Trace

| Formula ID | Source | Derivation | Destination | 状态 |
|---|---|---|---|---|
| F1 | 题面静水恢复力定义 + 圆形水线面 | `K_h=ρgπR^2` | 浮子垂荡方程 | closed |
| F2 | 牛顿第二定律 + 附加质量/兴波阻尼 | 浮子受力平衡 | `z_f` 状态方程 | closed |
| F3 | 牛顿第二定律 + PTO 内力反向 | 振子受力平衡 | `z_o` 状态方程 | closed |
| F4 | 题面幂律“阻尼系数”定义 | `c=a|v_r|^n` 与 `F_d=cv_r` 合成 | Case 2 阻尼力 | closed |

### 题面—数学—代码—输出语义闭环

| 题面对象/要求 | 数学对象 | 未来 Python 锚点 | 输出/验证 | 状态 |
|---|---|---|---|---|
| Q1 波浪激励 | `f cos(ωt)` | `wave_force(t)` | 两类结果工作簿 | closed |
| 附加质量/兴波阻尼 | `m_a,b` | 参数读取与单位检查 | 运行配置 + 质量门 | closed |
| 两类 PTO 阻尼 | `F_d` 两种定义 | `damper_force(...)` | 两个题目模板 | closed |
| 0.2 s 输出间隔 | 离散输出网格 | `t_eval` | 时间列 | closed |
| 论文指定时刻 | 10/20/40/60/100 s | 审计点提取 | 主工作簿摘要区 | closed |

### 结构化简与复杂度复审

- 未使用附件4“弹簧原长”的原因：本问坐标以静水静平衡位置为零点，弹簧静预压与重力/浮力常量平衡项已消去；增量弹簧力仅由相对位移变化决定。
- 附加质量、兴波阻尼只作用在浮子方程；振子不直接承受海水水动力。
- Q1 无优化、筛选、循环接受/拒绝等需论文展示的控制逻辑，因此 `algorithm_presentation=not_needed`，不制造装饰性伪代码。
- 状态维数仅 4 个一阶变量，采用高精度自适应 ODE 求解即可，不需要 CFD 或高阶多体数值平台。

### Model Challenge

**Model Reviewer（独立正向审查）**

- 题意匹配：通过。
- 变量/约束闭合：通过。
- 水动力、PTO 内力和静水恢复项：完整。
- `preprocessing_decision=not_needed`：与当前附件结构和模型需求一致。
- 可计算性：4 维一阶系统可直接进行 full-fidelity 数值积分。
- 验证计划：线性情形用独立增广状态空间矩阵指数核验；非线性情形用全时域步长加密复算比较。
- 结论：`passed`，无 blocking 项。

**Devil's Advocate（独立反向挑战）**

重点检查了：符号方向、静平衡常量项是否错误保留、附件4弹簧原长未进入增量方程是否合理、非线性阻尼是否把“阻尼系数”误当“阻尼力”、是否错误使用恰好 `40T` 作为 0.2 s 网格点、是否漏用附加质量/兴波阻尼/静水恢复力。

结论：当前模型可通过。残余警告仅为实现层必须保持统一的“向上为正”符号约定，并且时间输出严格保持 0.2 s 等间距，不为凑齐 `40T` 增加一个非 0.2 s 的末端点。

### Model Approval Brief

- research_object：Q1 浮子—振子两自由度垂荡系统。
- selected_model：以静平衡位置为原点的线性水动力两自由度 ODE；Case 2 仅 PTO 阻尼项保持题设幂律非线性。
- core_variables：`z_f,z_o,ż_f,ż_o`。
- objective：计算两个阻尼情形下前 40 个波浪周期的位移/速度时程，并形成题目要求结果文件与指定时刻数值。
- key_constraints：题面给定频率/参数；初值为零；时间间隔 0.2 s；不得删除附加质量、兴波阻尼、静水恢复和 PTO 内力。
- preprocessing_decision：`not_needed`。
- structural_simplification：静平衡增量坐标消去常量重力/浮力/弹簧预压；不作额外动力学降阶。
- solver_or_algorithm：用户本地运行 `scipy.integrate.solve_ivp(method='DOP853')`；`rtol=1e-10`、`atol=1e-12`、主积分 `max_step=T/200`；禁止静默 fallback。线性 Case 1 用增广状态空间矩阵指数独立核验；Case 2 用 `max_step=T/400` 加密复算核验。
- algorithm_presentation：`not_needed`。
- rejected_route_reason：不采用完整几何/流体高阶非线性模型，因为题目明确给定线性微幅波水动力参数，Q1 也没有纵摇几何耦合。
- residual_warnings：实现必须统一符号；时间网格末点为不超过 `40T` 的最后一个 0.2 s 网格点。
- next_stage_implementation_scope：Human Approval 通过后，只生成并静态检查 `问题一求解/问题一求解.py`；助手不运行赛题主求解代码。用户本地 full_fidelity 运行后返回主工作簿，再进入主结果质量门。

#### 结果摘要

- 当前状态：`pending`。
- 尚未生成或验收 Q1 主求解工作簿；不得在此记录正式数值。

## 待办与缺口

1. 2026-09-03 已发现上一轮显示的 `f240b7d5...` 并非仓库 `validate_semantic_governance.py` 对当前语义区实际计算出的 SHA-256；该值不得用于 Human Approval 绑定。
2. 当前可批准的 Q1 绑定为：`semantic revision=1 / hash=3c949a24b44d6d9ee12abe4e763e47942406456c523faf295263f06fac297192`。
3. 在用户对上述 current hash 重新显式批准前，不得生成正式 `问题一求解.py`。
4. Q2-Q4 尚未随本次锁模批准，后续分别完成语义闭环和 Challenge 后再进入对应锁模/求解阶段。
